import styled from "styled-components";

const StyledHeader = styled.header`
  background-color: var(--color-grey-0);
  padding: 2.4rem 3.6rem;
`;

const Header = () => {
  return <StyledHeader>heasder</StyledHeader>;
};

export default Header;
